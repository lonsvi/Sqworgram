<Page x:Class="_1ДЛЯ_ТЕСТА_ДИЗАЙНА_ПРОСТО.ThemeSettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d"
      Title="ThemeSettingsPage" d:DesignHeight="600" d:DesignWidth="1000">

    <Page.Resources>
        <Storyboard x:Key="PageEnterAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,30,0,0" To="0,0,0,0" Duration="0:0:0.4">
                <ThicknessAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </ThicknessAnimation.EasingFunction>
            </ThicknessAnimation>
        </Storyboard>
        <Style x:Key="ModernScrollThumb" TargetType="Thumb">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="rectangle" CornerRadius="3" Background="#33FFFFFF" Width="6" Margin="2,0"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="rectangle" Property="Background" Value="#88FFFFFF"/>
                                <Setter TargetName="rectangle" Property="Width" Value="8"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="rectangle" Property="Background" Value="{DynamicResource AccentColorBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ModernIconButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                        Background="{TemplateBinding Background}" 
                        CornerRadius="20" 
                        SnapsToDevicePixels="True"
                        Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#22FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#44FFFFFF"/>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ModernScrollBar" TargetType="ScrollBar">
            <Setter Property="Width" Value="12"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent" SnapsToDevicePixels="True">
                            <Track x:Name="PART_Track" IsDirectionReversed="true" IsEnabled="{TemplateBinding IsEnabled}">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ModernScrollThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#221C1C1C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CaretBrush" Value="{DynamicResource AccentColorBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#33FFFFFF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="10" BorderThickness="{TemplateBinding BorderThickness}" 
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ScrollViewer x:Name="PART_ContentHost" Padding="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentColorBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AccentColorBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ThemePresetButtonStyle" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid x:Name="btnRoot">
                            <Ellipse x:Name="border" Width="42" Height="42" Stroke="#44FFFFFF" StrokeThickness="2" Fill="{TemplateBinding Background}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Stroke" Value="White"/>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.1" ScaleY="1.1"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="Transparent">
        <Grid x:Name="BackgroundAnimationGrid">
            <Rectangle Fill="{DynamicResource PrimaryBackgroundColor}"/>
            <Rectangle>
                <Rectangle.Fill>
                    <RadialGradientBrush>
                        <GradientStop Color="#330A3A" Offset="0"/>
                        <GradientStop Color="#1A0B2E" Offset="0.5"/>
                        <GradientStop Color="#0F051A" Offset="1"/>
                    </RadialGradientBrush>
                </Rectangle.Fill>
            </Rectangle>
            <Border Width="500" Height="500" CornerRadius="250" Opacity="0.15" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-200,-150,0,0" Background="{DynamicResource Blob1Brush}"/>
            <Border Width="600" Height="600" CornerRadius="300" Opacity="0.12" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-250,-200" Background="{DynamicResource Blob2Brush}"/>
        </Grid>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#991C1C1C" BorderBrush="#22FFFFFF" BorderThickness="0,0,0,1" Padding="15,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="BackButton" 
        Click="BackButton_Click" 
        Style="{StaticResource ModernIconButton}"
        Margin="5,0,0,0">
                        <Path Data="M15.41 7.41L14 6l-6 6 6 6 1.41-1.41L10.83 12z" 
          Fill="White" 
          Stretch="Uniform" 
          Width="20"/>
                    </Button>
                    <TextBlock Grid.Column="1" Text="Настройка внешнего вида" FontSize="20" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center" Margin="15,0,0,0"/>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="25">

                <ScrollViewer.Resources>
                    <Style TargetType="ScrollBar" BasedOn="{StaticResource ModernScrollBar}"/>
                </ScrollViewer.Resources>
                <StackPanel MaxWidth="600" HorizontalAlignment="Center">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Style.Triggers>
                                <EventTrigger RoutedEvent="Loaded">
                                    <BeginStoryboard Storyboard="{StaticResource PageEnterAnimation}"/>
                                </EventTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>

                    <TextBlock Text="ГОТОВЫЕ ПРЕСЕТЫ" Foreground="White" Opacity="0.5" FontSize="10" FontWeight="Bold" Margin="10,0,0,12" HorizontalAlignment="Left"/>
                    <Border Background="#441C1C1C" BorderBrush="#33FFFFFF" BorderThickness="1.2" CornerRadius="20" Padding="25" Margin="0,0,0,25">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="25" Color="Black" Opacity="0.3" ShadowDepth="0"/>
                        </Border.Effect>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button Style="{StaticResource ThemePresetButtonStyle}" Tag="#FF6A359C;#FF4A00E0;#FF8E2DE2" Click="PresetButton_Click" ToolTip="Фиолетовая">
                                <Button.Background>
                                    <LinearGradientBrush>
                                        <GradientStop Color="#FF6A359C" Offset="0"/>
                                        <GradientStop Color="#FF421061" Offset="1"/>
                                    </LinearGradientBrush>
                                </Button.Background>
                            </Button>
                            <Button Style="{StaticResource ThemePresetButtonStyle}" Tag="#FF0077be;#FF00529B;#FF00A4E4" Click="PresetButton_Click" Margin="25,0,0,0" ToolTip="Океан">
                                <Button.Background>
                                    <LinearGradientBrush>
                                        <GradientStop Color="#FF0077be" Offset="0"/>
                                        <GradientStop Color="#FF00529B" Offset="1"/>
                                    </LinearGradientBrush>
                                </Button.Background>
                            </Button>
                            <Button Style="{StaticResource ThemePresetButtonStyle}" Tag="#FFF47C3C;#FFFF5F6D;#FFFFC371" Click="PresetButton_Click" Margin="25,0,0,0" ToolTip="Закат">
                                <Button.Background>
                                    <LinearGradientBrush>
                                        <GradientStop Color="#FFF47C3C" Offset="0"/>
                                        <GradientStop Color="#FFFF5F6D" Offset="1"/>
                                    </LinearGradientBrush>
                                </Button.Background>
                            </Button>
                            <Button Style="{StaticResource ThemePresetButtonStyle}" Tag="#FF2E7D32;#FF1B5E20;#FF4CAF50" Click="PresetButton_Click" Margin="25,0,0,0" ToolTip="Лес">
                                <Button.Background>
                                    <LinearGradientBrush>
                                        <GradientStop Color="#FF2E7D32" Offset="0"/>
                                        <GradientStop Color="#FF1B5E20" Offset="1"/>
                                    </LinearGradientBrush>
                                </Button.Background>
                            </Button>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="РУЧНАЯ НАСТРОЙКА ЦВЕТОВ" Foreground="White" Opacity="0.5" FontSize="10" FontWeight="Bold" Margin="10,0,0,12" HorizontalAlignment="Left"/>
                    <Border Background="#441C1C1C" BorderBrush="#33FFFFFF" BorderThickness="1.2" CornerRadius="20" Padding="25">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="25" Color="Black" Opacity="0.3" ShadowDepth="0"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="Цвет акцента (HEX)" Foreground="#AAFFFFFF" FontSize="11" Margin="5,0,0,8"/>
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="AccentColorTextBox" Style="{StaticResource ModernTextBox}" TextChanged="ColorTextBox_TextChanged"/>
                                <Border Grid.Column="1" Width="40" Height="40" Margin="10,0,0,0" CornerRadius="8" BorderBrush="#44FFFFFF" BorderThickness="1">
                                    <Rectangle x:Name="AccentColorPreview" RadiusX="7" RadiusY="7"/>
                                </Border>
                            </Grid>

                            <TextBlock Text="Верхний фон" Foreground="#AAFFFFFF" FontSize="11" Margin="5,0,0,8"/>
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="BlobColor1TextBox" Style="{StaticResource ModernTextBox}" TextChanged="ColorTextBox_TextChanged"/>
                                <Border Grid.Column="1" Width="40" Height="40" Margin="10,0,0,0" CornerRadius="8" BorderBrush="#44FFFFFF" BorderThickness="1">
                                    <Rectangle x:Name="BlobColor1Preview" RadiusX="7" RadiusY="7"/>
                                </Border>
                            </Grid>

                            <TextBlock Text="Нижний фон" Foreground="#AAFFFFFF" FontSize="11" Margin="5,0,0,8"/>
                            <Grid Margin="0,0,0,25">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="BlobColor2TextBox" Style="{StaticResource ModernTextBox}" TextChanged="ColorTextBox_TextChanged"/>
                                <Border Grid.Column="1" Width="40" Height="40" Margin="10,0,0,0" CornerRadius="8" BorderBrush="#44FFFFFF" BorderThickness="1">
                                    <Rectangle x:Name="BlobColor2Preview" RadiusX="7" RadiusY="7"/>
                                </Border>
                            </Grid>

                            <Button Content="Применить и сохранить" Style="{StaticResource ModernButton}" Click="SaveCustomTheme_Click"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>