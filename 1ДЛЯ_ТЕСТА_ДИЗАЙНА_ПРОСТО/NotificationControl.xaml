<UserControl x:Class="_1ДЛЯ_ТЕСТА_ДИЗАЙНА_ПРОСТО.NotificationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="300">

    <UserControl.Resources>
        <!-- Toast entrance animation -->
        <Storyboard x:Key="ToastSlideIn">
            <ThicknessAnimation Storyboard.TargetProperty="(FrameworkElement.Margin)" From="400,0,0,0" To="0" Duration="0:0:0.4">
                <ThicknessAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </ThicknessAnimation.EasingFunction>
            </ThicknessAnimation>
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <Storyboard x:Key="ToastSlideOut">
            <ThicknessAnimation Storyboard.TargetProperty="(FrameworkElement.Margin)" To="400,0,0,0" Duration="0:0:0.3">
                <ThicknessAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </ThicknessAnimation.EasingFunction>
            </ThicknessAnimation>
            <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.3"/>
        </Storyboard>
    </UserControl.Resources>

    <!-- Liquid Glass approx: Toast notification card -->
    <Border
        x:Name="NotificationBorder"
        Style="{StaticResource LiquidGlassCard}"
        CornerRadius="20"
        Padding="16,12"
        Margin="0">
        <Border.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard Storyboard="{StaticResource ToastSlideIn}"/>
            </EventTrigger>
        </Border.Triggers>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Icon (Success/Error) -->
            <Border Grid.Column="0" Width="28" Height="28" CornerRadius="14">
                <Grid>
                    <!-- Success Icon (Green checkmark) -->
                    <Path 
                        x:Name="SuccessIcon"
                        Data="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z" 
                        Fill="#22C55E" 
                        Stretch="Uniform"
                        Visibility="Visible"/>

                    <!-- Error Icon (Red X) -->
                    <Path 
                        x:Name="ErrorIcon"
                        Data="M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z" 
                        Fill="White" 
                        Stretch="Uniform"
                        Visibility="Collapsed"/>
                </Grid>
            </Border>

            <!-- Text Content -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0,0,0">
                <TextBlock 
                    x:Name="NotificationText"
                    Text="Успешно!" 
                    FontSize="14" 
                    FontWeight="Normal"
                    Foreground="{DynamicResource PrimaryTextColor}"
                    VerticalAlignment="Center"/>

                <TextBlock 
                    x:Name="NotificationDescription"
                    Text="" 
                    FontSize="12" 
                    Foreground="{DynamicResource SecondaryTextColor}"
                    Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Close Button -->
            <Button 
                Grid.Column="2"
                Style="{StaticResource LiquidGlassIconButton}"
                Padding="4"
                Click="CloseButton_Click"
                Margin="8,0,0,0">
                <Path 
                    Data="M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z" 
                    Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" 
                    Stretch="Uniform" 
                    Width="12"/>
            </Button>

            <!-- Progress Bar at Bottom -->
            <Rectangle 
                Grid.Column="0" Grid.ColumnSpan="3"
                x:Name="ProgressBar"
                Height="2" 
                VerticalAlignment="Bottom"
                Fill="{DynamicResource AccentColorBrush}"
                Margin="-16,-12,-16,0">
                <Rectangle.RenderTransform>
                    <ScaleTransform ScaleX="1" CenterX="0"/>
                </Rectangle.RenderTransform>
            </Rectangle>
        </Grid>
    </Border>
</UserControl>
