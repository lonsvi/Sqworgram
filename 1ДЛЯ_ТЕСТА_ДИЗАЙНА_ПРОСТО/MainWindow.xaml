<Window x:Class="_1ДЛЯ_ТЕСТА_ДИЗАЙНА_ПРОСТО.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sqworogram"
        WindowStartupLocation="CenterScreen"
        WindowState="Normal"
        Width="1000" Height="600"
        MinWidth="800" MinHeight="500"
        WindowStyle="SingleBorderWindow"
        AllowsTransparency="True" 
        Background="Transparent"
        Icon="/image (2).jpg"
        StateChanged="MainWindow_StateChanged">

    <Window.Resources>
        <!-- Liquid Glass approx: Translucent window control buttons with smooth animations -->
        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="44"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border 
                                x:Name="border" 
                                Background="Transparent" 
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                                <Border.RenderTransform>
                                    <ScaleTransform ScaleX="1" ScaleY="1" CenterX="22" CenterY="20"/>
                                </Border.RenderTransform>
                            </Border>
                            <ContentPresenter 
                                x:Name="contentPresenter" 
                                Focusable="False" 
                                HorizontalAlignment="Center" 
                                VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource LiquidGlassPrimaryBrush}"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation 
                                                Storyboard.TargetName="border" 
                                                Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" 
                                                To="1.08" Duration="0:0:0.2">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                            <DoubleAnimation 
                                                Storyboard.TargetName="border" 
                                                Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" 
                                                To="1.08" Duration="0:0:0.2">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation 
                                                Storyboard.TargetName="border" 
                                                Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" 
                                                To="1" Duration="0:0:0.15">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                            <DoubleAnimation 
                                                Storyboard.TargetName="border" 
                                                Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" 
                                                To="1" Duration="0:0:0.15">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource LiquidGlassSecondaryBrush}"/>
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Close Button with red accent ripple -->
        <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource WindowControlButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFE81123"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FFF1707A"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <WindowChrome.WindowChrome>
        <WindowChrome 
            ResizeBorderThickness="8"
            CaptionHeight="60"
            CornerRadius="28"
            GlassFrameThickness="0"
            UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <!-- Main Liquid Glass Container -->
    <Border 
        x:Name="MainWindowBorder" 
        CornerRadius="28" 
        ClipToBounds="True"
        Background="{DynamicResource PrimaryBackgroundColor}"
        BorderBrush="{StaticResource LiquidGlassBorderBrush}"
        BorderThickness="1">
        <Border.Effect>
            <!-- Soft shadow for glass depth (как в iOS) -->
            <DropShadowEffect 
                ShadowDepth="0" 
                BlurRadius="60" 
                Opacity="0.35" 
                Color="#000000"/>
        </Border.Effect>

        <!-- Main Content Grid -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title Bar with Liquid Glass dragging surface -->
            <Border 
                Grid.Row="0" 
                Background="{StaticResource LiquidGlassPrimaryBrush}"
                BorderBrush="{StaticResource LiquidGlassBorderBottom}"
                BorderThickness="0,0,0,1"
                CornerRadius="28,28,0,0"
                MouseDown="Border_MouseDown"
                Padding="16,8">
                <Grid>
                    <!-- App Icon & Title -->
                    <StackPanel 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Left" 
                        VerticalAlignment="Center">
                        <Image 
                            Source="/image (2).jpg" 
                            Width="24" 
                            Height="24"
                            RenderOptions.BitmapScalingMode="HighQuality"
                            Opacity="0.9"
                            Margin="0,0,8,0"/>
                        <TextBlock 
                            Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" 
                            Foreground="{DynamicResource PrimaryTextColor}" 
                            VerticalAlignment="Center"
                            FontSize="14"
                            FontWeight="SemiBold"
                            FontFamily="Segoe UI Variable, Segoe UI"/>
                    </StackPanel>

                    <!-- Window Control Buttons -->
                    <StackPanel 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center">
                        <Button 
                            Style="{StaticResource WindowControlButton}" 
                            Click="MinimizeButton_Click" 
                            WindowChrome.IsHitTestVisibleInChrome="True"
                            ToolTip="Minimize"
                            Margin="0,0,4,0">
                            <Path 
                                Data="M2,8 H14" 
                                Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" 
                                StrokeThickness="1.5"/>
                        </Button>
                        <Button 
                            Style="{StaticResource WindowControlButton}" 
                            Click="MaximizeRestoreButton_Click" 
                            WindowChrome.IsHitTestVisibleInChrome="True"
                            ToolTip="Maximize"
                            Margin="0,0,4,0">
                            <Path 
                                Data="M2,3 H14 V14 H2 Z" 
                                Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" 
                                StrokeThickness="1.5"/>
                        </Button>
                        <Button 
                            Style="{StaticResource CloseButton}" 
                            Click="CloseButton_Click" 
                            WindowChrome.IsHitTestVisibleInChrome="True"
                            ToolTip="Close">
                            <Path 
                                Data="M3,3 L13,13 M3,13 L13,3" 
                                Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" 
                                StrokeThickness="1.5"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Content Frame -->
            <Border 
                Grid.Row="1"
                CornerRadius="0,0,28,28"
                Background="{DynamicResource PrimaryBackgroundColor}">
                <Frame 
                    x:Name="MainFrame" 
                    NavigationUIVisibility="Hidden"
                    Margin="0"/>
            </Border>

            <!-- Notifications Container (Liquid Glass Toast) -->
            <StackPanel 
                x:Name="NotificationArea" 
                Grid.Row="1" 
                VerticalAlignment="Top" 
                HorizontalAlignment="Right" 
                Margin="0,16,16,0" 
                Panel.ZIndex="999"/>
        </Grid>
    </Border>
</Window>
